on: 
    push:
    workflow_dispatch:

jobs: 
    ubuntu-echo:
        runs-on: ubuntu-22.04
        steps:
            - run: echo "Hello from ubuntu"

    windows-echo: 
        runs-on: windows-2025
        
        steps: 
            - name: "Windows"
              run: echo 'HWEAST'
              shell: bash
            - name: 'asdasda'
              shell: bash
              run: echo $RUNNER_OR

    mac-echo: 
        runs-on: macos-26
        steps: 
            - run: echo "Hello from mac"