name: Workflow events

on:
  push:
  schedule:
    - cron: "*/5 * * * *" 
  workflow_dispatch: 
  pull_request:
    

jobs:
  printing-events:
    runs-on: ubuntu-latest
    steps:
      - name: Show Triggered events
        run: echo "Event ${{github.event_name}} triggered"
